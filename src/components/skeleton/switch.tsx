"use client"

import * as React from "react"
import { Skeleton } from "../ui/skeleton"

export const SkeletonSwitch = () => {
  return <Skeleton className="inline-flex h-6 w-11 shrink-0 rounded-full" />
}
